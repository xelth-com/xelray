# docs/

Place `screenshot.png` here — a full-window capture of a loaded CT series
(sidebar, canvas, toolbar) — and it will appear in the root README.

Use anonymised or synthetic data for anything committed to this repository.
